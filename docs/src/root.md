---
title: root
---

<div class="lead">Path to the repository root</div>

`const gitRoot = repo.root;`

This returns the initial `filepath` used when creating the instance through `new
Gilmore(filepath)`.
